apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmspec.fullname" . }}
  labels:
    {{- include "pmspec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pmspec.selectorLabels" . | nindent 4 }}
